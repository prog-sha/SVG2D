# エージェント待機の違反

`collaboration.wait_agent timeout_ms=1000` を実行したところ、道具の最小値により 10 秒待機へ補正された。

以後は作業を進めながら通知を受け取り、短い待機を指定できない道具を使わない。

再確認で `collaboration.wait_agent timeout_ms=10000` を実行し、10秒待機した。以後、完了確認は待機せず `list_agents` を使い、その間は差分確認や公開準備を進める。
